{
  "name": "@fx/monorepo",
  "version": "0.0.1",
  "description": "developer automation",
  "scripts": {
    "build": "pnpm run build -r --if-present",
    "clean": "pnpm run clean -r --if-present",
    "fx": "fx",
    "dfx": "node --inspect node_modules/@fx/cli/build/main.js",
    "test": "fx test",
    "dev": "fx dev"
  },
  "engines": {
    "node": "=16",
    "pnpm": "=6"
  },
  "devDependencies": {
    "@fx/cli": "workspace:^0.0.1",
    "@fx/core": "workspace:^0.0.1",
    "@fx/teams": "workspace:^0.0.1",
    "@fx/templates": "workspace:^0.0.1",
    "change-case": "^4.1.2",
    "typescript": "4.5.5",
    "zod": "^3.11.6"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zhenyasav/fx.git"
  }
}