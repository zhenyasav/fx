{
  "name": "@nice/fx-repo",
  "version": "1.0.0",
  "description": "developer automation",
  "scripts": {
    "build": "pnpm run build -r --if-present",
    "build:tsc": "pnpm run build:tsc -r --if-present",
    "clean": "pnpm run clean -r --if-present",
    "fx": "pnpm exec fx"
  },
  "engines": {
    "node": "=16",
    "pnpm": "=6"
  },
  "type": "module",
  "devDependencies": {
    "@fx/cli": "workspace:^1.0.0",
    "@fx/teams": "workspace:^1.0.0",
    "@fx/templates": "workspace:^1.0.0",
    "change-case": "^4.1.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zhenyasav/fx.git"
  }
}
