{
  "name": "@nice/fx-repo",
  "version": "1.0.0",
  "description": "developer automation",
  "scripts": {
    "build": "pnpm run build -r --if-present --filter @nice/*",
    "fx": "pnpm exec fx"
  },
  "engines": {
    "node": "=16",
    "pnpm": "=6"
  },
  "type": "module",
  "devDependencies": {
    "@nice/fx": "workspace:^1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zhenyasav/fx.git"
  }
}
