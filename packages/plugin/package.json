{
  "name": "@fx/plugin",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build && tsc --build --clean"
  },
  "dependencies": {
    "@nice/file": "workspace:^1.0.0",
    "@fx/zod-inquirer": "workspace:^1.0.0",
    "zod": "^3.11.6"
  }
}