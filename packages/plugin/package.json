{
  "name": "@fx/plugin",
  "version": "0.1.6",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "clean": "rm -rf build && tsc --build --clean"
  },
  "dependencies": {
    "@fx/zod-inquirer": "workspace:^0.1.0",
    "@nice/file": "workspace:^0.1.0",
    "zod": "^3.11.6"
  }
}