{
  "name": "@fx/templates",
  "version": "0.1.1",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build && tsc --build --clean",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@fx/plugin": "workspace:^0.1.0",
    "@fx/zod-inquirer": "workspace:^0.1.0",
    "@nice/plate": "workspace:^0.1.1",
    "zod": "^3.11.6"
  }
}