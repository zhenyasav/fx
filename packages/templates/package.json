{
  "name": "@fx/templates",
  "version": "0.0.1",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build && tsc --build --clean",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@fx/plugin": "workspace:^0.0.1",
    "@fx/zod-inquirer": "workspace:^0.0.1",
    "@nice/plate": "workspace:^0.0.1",
    "zod": "^3.11.6"
  }
}