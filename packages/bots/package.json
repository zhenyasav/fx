{
  "name": "@fx/bots",
  "version": "0.1.0",
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf build && tsc --build --clean",
    "build": "tsc"
  },
  "dependencies": {
    "@fx/plugin": "workspace:^0.1.0",
    "@fx/tunnel": "workspace:^0.1.0"
  }
}
