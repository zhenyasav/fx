{
  "name": "@nice/fx-teams",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "main": "build/index.js",
  "publishConfig": {
    "main": "build/index.ts",
    "typings": "build/index.d.ts"
  },
  "scripts": {
    "build": "node scripts/build.js && tsc --emitDeclarationOnly",
    "build:dts": "tsc --emitDeclarationOnly",
    "build:tsc": "tsc --build",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@nice/ts-template": "workspace:^1.0.0",
    "esbuild": "^0.14.0",
    "glob": "^7.2.0"
  }
}